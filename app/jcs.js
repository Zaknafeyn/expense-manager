(function (window) {
    'use strict';

    var jcs = window.jcs = window.jcs || {};

    jcs.modules = {
        app: {
            name: 'myApp',
            router: 'ngRoute'
//            router: 'ui.router'
        }
    };
}(window));