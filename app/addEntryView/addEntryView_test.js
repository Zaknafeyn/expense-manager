'use strict';

describe('myApp.addEntryView module', function() {

  beforeEach(module('myApp.addEntryView'));

  describe('addEntryView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var addEntryViewCtrl = $controller('addEntryViewCtrl');
      expect(addEntryViewCtrl).toBeDefined();
    }));

  });
});